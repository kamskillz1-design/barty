{
  "name": "Listing",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the listing \u2014 what is being offered"
    },
    "description": {
      "type": "string",
      "description": "Detailed description"
    },
    "intent": {
      "type": "string",
      "enum": [
        "offering",
        "seeking"
      ],
      "default": "offering",
      "description": "Whether the user is offering this item/service or seeking it"
    },
    "seeking_interests": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "For offering listings: specific items/services the user wants in return. Empty when is_seeking_anything is true."
    },
    "is_seeking_anything": {
      "type": "boolean",
      "default": false,
      "description": "When true, the owner is open to any offer in return (Mode B / Open Exchange). seeking_interests is ignored."
    },
    "item_seeking_title": {
      "type": "string",
      "description": "Optional short title describing what is sought (when specific). Nullable when is_seeking_anything is true."
    },
    "item_seeking_description": {
      "type": "string",
      "description": "Optional detailed description of what is sought (when specific). Nullable when is_seeking_anything is true."
    },
    "type": {
      "type": "string",
      "enum": [
        "good",
        "service"
      ],
      "description": "Whether this is a physical good or a service"
    },
    "category": {
      "type": "string",
      "description": "Listing category (e.g. electronics, clothing, tutoring)"
    },
    "country": {
      "type": "string",
      "description": "Country where the listing is located"
    },
    "city": {
      "type": "string",
      "description": "City or town"
    },
    "town": {
      "type": "string",
      "description": "Village or local area"
    },
    "language": {
      "type": "string",
      "description": "Language the listing owner communicates in"
    },
    "baseline_value": {
      "type": "integer",
      "description": "Internal neutral integer used for fairness matching \u2014 never displayed to users",
      "default": 50
    },
    "image_urls": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Image URLs for the listing"
    },
    "status": {
      "type": "string",
      "enum": [
        "available",
        "reserved",
        "traded"
      ],
      "default": "available",
      "description": "Availability status"
    },
    "offering_user_id": {
      "type": "string",
      "description": "User who owns this listing"
    }
  },
  "required": [
    "title",
    "type",
    "category",
    "baseline_value"
  ]
}